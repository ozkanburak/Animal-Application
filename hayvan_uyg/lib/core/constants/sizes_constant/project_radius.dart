import 'package:flutter/material.dart';

class ProjectRadius {
  static BorderRadius mainAllRadius = BorderRadius.circular(24);
  static BorderRadius allRadius = BorderRadius.circular(20);
  static BorderRadius buttonAllRadius = BorderRadius.circular(20);
}
